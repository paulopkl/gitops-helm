image:
  tag: prod-cc17d11

env:
  COLOR: RED

ingress:
  annotations:
    rewrite_target:
      enabled: false
      value: /
    app_root:
      enabled: false
      value:
  rules:
  - host: api-prod
    domain: paulo.local
    path: /
