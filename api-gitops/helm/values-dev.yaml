image:
  tag: dev-1b79bbc2

env:
  COLOR: YELLOW

ingress:
  annotations:
    rewrite_target:
      enabled: false
      value: /
    app_root:
      enabled: false
      value:
  rules:
  - host: api-dev
    domain: paulo.local
    path: /
